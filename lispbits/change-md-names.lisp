;; Open the directory
;; Find the files end with .md
;; Save the file paths into a list.
;; Access each list item and change the pathname/filename.
;; Change filename by replacing spaces with dash.

(defun open-dir (path))
